type StakingReward @entity {
  id: ID! #blockHeight-eventIdx
  #account: String!
  account: SumReward!
  balance: BigInt!
  date: Date!
  blockHeight: Int!
}

type SumReward @entity{
  id: ID! # AccountId
  totalReward: BigInt!
  blockheight: Int!
}

